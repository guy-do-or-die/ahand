/**
 * JS mirror of styles/tokens.css for the rare API that needs a literal color
 * (e.g. Privy `appearance.accentColor`). Keep in sync with tokens.css —
 * these two files are the only places a raw color may appear.
 */
export const AMBER_HEX = "#f5b841";
export const POCKET_EMOJI = "👤";
